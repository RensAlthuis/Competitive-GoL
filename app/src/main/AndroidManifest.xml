<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rens.competitive_gol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.MainActivity" />
        <activity android:name=".Controller.OnlineActivity" />
        <activity android:name=".Controller.RoomActivity" />
        <activity android:name=".Controller.AiActivity" />
        <activity android:name=".Controller.SettingsActivity" />
        <activity android:name=".Controller.TutorialActivity" />
        <activity android:name=".Controller.RatingActivity" />
        <activity android:name=".Controller.FeedBackActivity" />
        <activity android:name=".Controller.AboutActivity"></activity>
    </application>

</manifest>